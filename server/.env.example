# Copy to .env and fill in. Never commit real secrets.
APP_ENV=development
PORT=3000
# Comma-separated browser/dev origins (Expo web / Metro)
CORS_ORIGINS=http://localhost:8081,http://127.0.0.1:8081
# DATABASE_URL=
# JWT_SECRET=
