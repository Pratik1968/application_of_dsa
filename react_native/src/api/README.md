# `src/api/`

HTTP client for the monorepo `server/`.

- Base URL: `EXPO_PUBLIC_API_URL` (see `../.env.example`)
- Use `apiFetch` / `getApiBaseUrl` from `client.ts` — do not hardcode hosts in screens.
